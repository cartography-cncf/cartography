# AIBOM

The AIBOM module maps AI agent inventory onto production container images. It preserves scan provenance, source-level structure, component occurrences, workflow context, and query-friendly component relationships such as `USES_TOOL` and `USES_MODEL`.

```{toctree}
config
schema
```
