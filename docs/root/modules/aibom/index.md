# AIBOM

The AIBOM module maps AI agent inventory onto production container images. It preserves scan provenance, source-level structure, component occurrences, workflow context, and source-defined relationships between components.

```{toctree}
config
schema
```
