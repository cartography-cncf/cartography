# Wiz

```{toctree}
:hidden:

config
schema
```

Cartography can ingest Wiz issues, vulnerability findings, detection findings,
and failing configuration findings from the Wiz GraphQL API.

Wiz issues and findings are labeled as `Risk` and `SecurityIssue` to support
cross-provider security finding queries.
