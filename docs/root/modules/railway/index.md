# Railway

```{toctree}
config
schema
```

Registry-backed services are linked to the provider-neutral
[external container image schema](../external_container_images/index.md). Source-built
services without an explicit image reference remain separate from registry resolution.
