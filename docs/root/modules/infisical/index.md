# Infisical

```{toctree}
config
schema
```

Cartography ingests Infisical organizations and project metadata. The module
creates `InfisicalOrganization` and `InfisicalProject` nodes without requesting
or storing secret values.
